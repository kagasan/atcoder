#include "bits/stdc++.h"
using namespace std;
typedef long long ll;

int main(){
    
    ll N, K;
    cin >> N >> K;
    if(N % K == 0)cout << 0 << endl;
    else cout << 1 << endl;

    return 0;
}
