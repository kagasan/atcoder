#include "bits/stdc++.h"
using namespace std;
typedef long long ll;

int main(){
    
    ll a, b, c;
    cin >> a >> b >> c;
    if(a + b >= c)cout << "Yes" << endl;
    else cout << "No" << endl;

    return 0;
}
